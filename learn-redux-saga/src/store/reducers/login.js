import * as types from '../action-types';

const initState = {
  username: ''
};

export default function counter(state = initState, action) {
  switch (action.type) {

    default:
      return state;
  }
};
